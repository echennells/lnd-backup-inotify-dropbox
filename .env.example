# LND Backup Docker Configuration

# Required: Dropbox API token
DROPBOX_TOKEN=your_dropbox_token_here

# Network (will be auto-detected from LND config if not set)
NETWORK=mainnet
# Options: mainnet, testnet, signet, signet-mutinynet, regtest

# Local paths to mount
LND_DATA_DIR=./lnd-data
TAPD_DATA_DIR=./tapd-data

# Backup settings
CHECK_INTERVAL=300
LOG_LEVEL=info
ENABLE_TAPD=false

# For production, you might want:
# LND_DATA_DIR=/var/lib/lnd
# TAPD_DATA_DIR=/var/lib/tapd