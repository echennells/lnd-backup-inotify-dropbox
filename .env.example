# LND Backup Configuration

# Storage Connection String (required)
# Format: <provider>://<host>/<container_or_bucket>?<auth_params>
# Examples:
#   Azure: azure://account.blob.core.windows.net/container?sp=rw&st=...&sig=...
#   S3:    s3://bucket-name/path?region=us-east-1&access_key=...&secret_key=...
#   GCS:   gcs://bucket-name/path?project=my-project&key_file=...
STORAGE_CONNECTION_STRING=azure://your-account.blob.core.windows.net/your-container?sp=racwl&st=...&se=...&sig=your-sas-token

# Network (will be auto-detected from LND config if not set)
NETWORK=mainnet
# Options: mainnet, testnet, signet, signet-mutinynet, regtest

# Local paths to mount
LND_DATA_DIR=./lnd-data
TAPD_DATA_DIR=./tapd-data

# Backup settings
CHECK_INTERVAL=300
LOG_LEVEL=info
ENABLE_TAPD=false
BACKUP_DIR=/lightning-backups
LOCAL_BACKUP_DIR=/var/backup/lnd
KEEP_LAST_N_BACKUPS=30

# System identifier for multi-node setups
# Set to your hostname or custom identifier for meaningful folder names
SYSTEM_ID=

# For production, you might want:
# LND_DATA_DIR=/var/lib/lnd
# TAPD_DATA_DIR=/var/lib/tapd